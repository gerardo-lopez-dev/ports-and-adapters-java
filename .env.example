# Factor III — Config via environment variables.
# Copy to .env and fill in for your environment.
# NEVER commit .env with real values.

SPRING_PROFILES_ACTIVE=local
SERVER_PORT=8080

# Database (used by dev/prod profiles)
DB_URL=jdbc:postgresql://localhost:5432/appdb
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=appdb
